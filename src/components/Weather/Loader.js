import React from'react'; 
import { Bars } from  'react-loader-spinner'

const NotFoundError = (props) => {
  return (<Bars color="#00BFFF" height={80} width={80} />);
}
export default NotFoundError;
